. Address (already implemented, worth tightening)
Metrics you already use

spine_angle

wrist_angle / wrist_hinge

stance_width_ratio

How to tighten to “scientific” style

For stance_width_ratio, instead of fixed 0.9–2.2, reference the “shoulder‑width rule” and map that via your config (e.g., stance_width_ratio.acceptable = (0.8, 1.4); ideal = (0.9, 1.2)).

For spine_angle.address_ideal, set a band around typical “forward tilt” (≈40–50°) from biomechanics‑style sources.

For wrist_angle (used as grip‑proxy), define wrist_angle.address_acceptable and keep address_ideal slightly neutral.

Keep your hard‑coded rule for stance_width_ratio (0.9–2.2 → 100 else 60); just view it as a temporary fallback until you swap it to config‑driven bands.

2. Takeaway (currently default_score = 75)
Add these metrics (you likely already expose them in angles.py)

shoulder_club_angle (plane of takeaway)

wrist_hinge (early wrist set)

hip_lateral_shift (early side‑slide)

head_lateral (head sway)

Config keys to add under SCORING_THRESHOLDS

shoulder_club_angle.takeaway_ideal

wrist_hinge.takeaway_ideal

hip_lateral_shift.takeaway_ideal

head_lateral.takeaway_ideal

Scoring logic (in your new score_takeaway method)

For each metric, call your existing score_metric(...) using acceptable/ideal from scoring_config.py.

Optionally add one hard rule:

head_lateral < 3 pixels → 100, else 60 (mirroring Top / Impact).

Weight‑normalize using METRIC_WEIGHTS.takeaway (you already have METRIC_WEIGHTS style; just add a takeaway key).

This keeps your scoring machinery unchanged while turning Takeaway from placeholder into a real, metric‑based phase.

3. Mid‑backswing (currently default_score = 75)
Add these metrics

x_factor (shoulder − hip rotation at mid‑backswing)

left_elbow_angle

right_arm_angle

head_lateral (still low head movement)

Config keys

x_factor.mid_backswing_ideal

x_factor.mid_backswing_acceptable

left_elbow_angle.mid_backswing_ideal

right_arm_angle.mid_backswing_ideal

head_lateral.mid_backswing_ideal

Implementation sketch

Create score_mid_backswing(...) that:

Pulls metrics from angles.py.

Calls score_metric on each with SCORING_THRESHOLDS and its *.mid_backswing_* keys.

Applies METRIC_WEIGHTS.mid_backswing in phase_scorer.py:385‑style normalization.

Biomechanics‑wise, this captures early coil quality and prep for Top without over‑engineering.

4. Top (already implemented)
Your current metrics are excellent and already biomechanically sound:

shoulder_rotation (coil)

spine_angle (posture)

head_displacement (stability)

wrist_angle (cock)

What to fix / tighten

In scoring_config.py, set:

shoulder_rotation.backswing_ideal ≈ 120–140° typical elite range (calibrated per your coordinate system).

spine_angle.top_ideal close to address‑tilt plus a small bend.

wrist_angle.top_ideal in a neutral‑cock window (e.g., slightly bent but not cupped/bowed at extremes).

Keep head_disp < 5 → 100, else 70; that’s a simple, defensible stability rule.

No structural change needed; just tune the numbers in config to match biomechanics / Pro‑Am spread (e.g., “swing performance index” range 100 ± 10 for pros).

5. Mid‑downswing (already implemented)
You already have the right variables:

lag_angle / wrist_angle / wrist_hinge

hip_rotation / hip_angle

x_factor

kinematic_data (sequence timing)

Config keys you already use

lag_angle.mid_downswing_ideal

hip_rotation.downswing_ideal

x_factor.mid_downswing_ideal

kinematic timing bounds in scoring_config.py:193

What to keep

Keep your score_kinematic_sequence(...) logic at phase_scorer.py:397; it’s aligned with “pelvis → thorax → arm → club” sequence that literature ties to power and consistency.

The only “missing” piece is making x_factor.mid_downswing_ideal reflect “X‑Factor stretch” (dynamic increase rather than just big static separation), which you can capture by looking at Δx_factor across early downswing windows.

6. Impact (already implemented)
You already score:

Lag release (lag_angle / wrist_angle)

x_factor unwind

arm_extension / lead_arm_angle

head_displacement

Hard‑coded rules you already have

arm extension 160–180 → 100, else 70

head_disp < 3 → 100, else 60

Both are biomechanically reasonable:

Full(ish) arm extension at impact correlates with better strike and speed.

Minimal head sway improves consistency.

Additional tweak

Add impact_ideal bands for lag_angle, wrist_angle, x_factor in scoring_config.py and reuse them in Impact (even if you already have *.impact_ideal = ...). This keeps Takeaway / Mid‑backswing consistent with the same naming style you use here.

7. Follow‑through (currently default_score = 75)
Add metrics (you likely already compute them)

weight_shift_index (e.g., COM‑to‑stance ratio or ankle‑based indicator)

left_knee_angle

trunk_rotation / hip_rotation

spine_angle (similar to address)

Config keys

weight_shift_index.follow_through_ideal

left_knee_angle.follow_through_ideal

trunk_rotation.follow_through_ideal

spine_angle.follow_through_ideal

Implementation

Create score_follow_through(...) that:

Pulls these metrics.

Calls score_metric(...) with SCORING_THRESHOLDS using *.follow_through_*.

Applies METRIC_WEIGHTS.follow_through in your existing weighted‑normalization.

This gives you a balance‑ and rotation‑focused phase without changing your scoring logic.

8. Finish (currently default_score = 75)
Metrics

body_balance (COM‑in‑stance, sway‑index)

spine_angle (forward tilt close to address)

facing_angle (body aligned with target line)

Config keys

body_balance.finish_ideal

spine_angle.finish_ideal

facing_angle.finish_ideal

Implementation

Create score_finish(...) with:

score_metric(..., body_balance.finish_ideal, ...)

Same for spine_angle and facing_angle.

METRIC_WEIGHTS.finish in normalization.

This keeps your 8‑phase structure fully driven by metrics + config, with Finish capturing “static stability” similar to Address but at the end.

